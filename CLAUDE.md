# Project preferences

This repo deploys to **Cloudflare** (Workers Assets) on every push to `main`.
Live URL: https://katieandryan.co.uk/

Just push to main — deploys are fast (~10s) and free. No staging branch,
no preview environment needed.

The `staging` branch is unused and can be deleted.
