# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/

# Node / npm (in case we add tooling later)
node_modules/
npm-debug.log*

# Netlify CLI state (machine-local; don't commit)
.netlify/

# Environment / secrets
.env
.env.*

# Design tool sidecar (image-slot persistence — for design tool only)
.image-slots.state.json

# Build artifacts (none right now, but future-proof)
dist/
build/

# Design artifacts — kept local only, not part of the deployed site
project/
chats/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
